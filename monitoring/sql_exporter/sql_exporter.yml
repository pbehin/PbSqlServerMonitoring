# Auto-generated sql_exporter configuration
global:
  scrape_timeout: 10s
  min_interval: 5s

collector_files:
  - /etc/sql_exporter/mssql_standard.collector.yml

jobs:
  - job_name: default
    collectors: [mssql_standard]
    static_configs:
      - targets:
          dummy: 'sqlserver://sa:dummy@dummy-host:1433'
        labels:
          connection_id: '0000000000000000'
          connection_name: 'Default Dummy'
          server: 'dummy-host'
          database: 'master'
          user_id: 'system'

